# Shared library for altair Python code.
import setuptools

setuptools.setup(
    name="altair",
    packages=["altair"],
    # Ideally, this should be filled out, but I can't be bothered.
    install_requires=[],
)
