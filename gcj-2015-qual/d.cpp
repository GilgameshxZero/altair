#include <iostream>
#include <cstdio>
using namespace std;

int main ()
{
	freopen ("in.txt", "r", stdin);
	freopen ("out.txt", "w", stdout);

	int T;
	cin >> T;

	for (int t = 1;t <= T;t++)
	{
		cout << "Case #" << t << ": ";
		int x, r, c;
		cin >> x >> r >> c;

		if (x == 1)
			cout << "GABRIEL\n";
		else if (x == 2)
		{
			if (r % 2 == 1
		}
		else if (x == 3)
		{

		}
	}

	return 0;
}